package factoryMethod;

public class AboutFactoryMethod {

    /*
    FactoryMethod 패턴은 TemplateMethod 패턴의 전형적인 응용이다.
    인스턴스 생성 방법을 상위 클래스에서 결정하되, 구체적인 클래스 이름은 결정하지 않는다.
    구체적인 것은 모두 하위클래스에서 설정한다.
    인스턴스 생성을 위한 뼈대(framework)와 실제 인스턴스를 생성하는 클래스를 나누어 생각할 수 있게 만든다.
    */
}
